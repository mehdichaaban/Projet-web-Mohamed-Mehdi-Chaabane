version: "3.8"

services:
  db:
    image: postgres:16
    container_name: ice_db
    environment:
      POSTGRES_DB: ice_shop
      POSTGRES_USER: ice_user
      POSTGRES_PASSWORD: ice_pass
    ports:
      - "5432:5432"
